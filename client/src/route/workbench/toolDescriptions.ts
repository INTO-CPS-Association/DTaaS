/**
 * One line saying what each workbench tool is.
 *
 * The keys are the ones `useWorkbenchLinkValues` produces. A key with no entry
 * renders a card without a description, so adding a tool never requires
 * editing this file first.
 */
const toolDescriptions: Readonly<Record<string, string>> = {
  VNCDESKTOP: 'A full Linux desktop, in the browser.',
  VSCODE: 'The editor, with your workspace already open.',
  JUPYTERLAB: 'Notebooks, terminals and a file browser.',
  JUPYTERNOTEBOOK: 'The classic notebook interface.',
  LIBRARY_PREVIEW:
    'The library page on its own, without the platform around it.',
  DT_PREVIEW:
    'The digital twins page on its own, without the platform around it.',
};

export default toolDescriptions;
