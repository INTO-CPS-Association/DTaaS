yaml-files:
  - '*.yaml'
  - '*.yml'
  - '.yamllint'

extends: default

rules:
  document-start:
    present: false
  truthy:
    allowed-values: ['True', 'true', 'False', 'false']
    check-keys: true
