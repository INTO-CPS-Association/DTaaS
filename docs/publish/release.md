## Download Package

The software is available as a
[zip package](URL).
The package should be downloaded and unzipped. A new **VERSION** folder is
created. The remaining installation instructions assume the use of a
Windows/Linux/MacOS terminal in the **VERSION** folder.
