# Generated DTaaS packages

This directory contains generated self-contained installation packages:

1. `localhost`
2. `secure-localhost`
3. `server`
4. `secure-server`

Regenerate packages using:

```bash
cd deploy/dtaas
python scripts/build-packages.py --clean
```
