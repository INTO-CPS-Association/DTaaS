[[runners]]
  name = "dtaas-runner-0"
  url = "https://foo.com/gitlab/"
  token = "xxx"
  executor = "docker"
  [runners.docker]
    tls_verify = false
    image = "ruby:2.7"
    privileged = false
    disable_entrypoint_overwrite = false
    oom_kill_disable = false
    disable_cache = false
    volumes = ["/cache"]
